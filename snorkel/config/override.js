// this override.js is ALWAYS applied
// before the ENV level config
// and after the config/config.js

module.exports = {
//  sockets: false,
  behind_proxy: true,
  require_https: false,
//  separate_services: false,
//  no_api_auth: true,
//  use_cls: false, // use continuation local storage
//  default_max_dataset_size: 1024 * 1024 * 100
};
